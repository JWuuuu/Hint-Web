{
  "name": "hint-web",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --dir web dev:web",
    "build": "pnpm --dir web build:web",
    "typecheck": "pnpm --dir web typecheck:web",
    "serve": "pnpm --dir web serve:web"
  }
}
