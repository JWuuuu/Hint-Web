{
  "name": "hint-workspace",
  "version": "0.0.0",
  "packageManager": "pnpm@11.5.0",
  "license": "MIT",
  "scripts": {
    "dev:api": "pnpm --filter @workspace/api-server run dev",
    "dev:web": "pnpm --filter @workspace/hint run dev",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "build:api": "pnpm --filter @workspace/api-server run build",
    "build:web": "pnpm --filter @workspace/hint run build",
    "db:push": "pnpm --filter @workspace/db run push",
    "start:api": "pnpm --filter @workspace/api-server run start",
    "serve:web": "pnpm --filter @workspace/hint run serve",
    "typecheck:api": "pnpm --filter @workspace/api-server run typecheck",
    "typecheck:web": "pnpm --filter @workspace/hint run typecheck",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.8.3",
    "typescript": "~5.9.3"
  }
}
